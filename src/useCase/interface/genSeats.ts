interface GenSeats {
    generateSeatNumbers(row1: string, row1Count: number,
        row2: string, row2Count: number, ticketCount: number,
        row1Array: number[], row2Array: number[]): [];
}
export default GenSeats;
